This is a student name management system, 
which uses the storage structure of sequence table and linked list,
 implements several functions for manipulating data structures, and integrates them into a system.